{
"buildCommand": "cd safefind-frontend && npm install && npm run build",\n  "outputDirectory": "safefind-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
