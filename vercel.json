{
  "buildCommand": "cd rakshak-frontend && npm install && npm run build",
  "outputDirectory": "rakshak-frontend/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
